config:
  tick_len: 10000  # tick length in microseconds (1s)
  num_ticks: 10  # number of ticks to run for
  source_init_ticks: 1  # number of source ticks run before time t=0

modules:
  counter_simple:
    language: python
    constructor: true